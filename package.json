{
  "name": "ppt-repo",
  "version": "1.0.0",
  "description": "在线PPT资源管理仓库，使用Reveal.js和GitHub Pages",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/generate-index.js",
    "dev": "npx live-server --port=8080 --open=/",
    "deploy": "npm run build && git add . && git commit -m 'Update presentations' && git push"
  },
  "keywords": ["ppt", "presentation", "reveal.js", "github-pages"],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
